name: VoiceCoach
options:
  bundleIdPrefix: com.voicecoach
  deploymentTarget:
    iOS: "17.0"
    macOS: "14.0"
  xcodeVersion: "16.0"
  generateEmptyDirectories: true

settings:
  SWIFT_VERSION: "6.0"
  SWIFT_STRICT_CONCURRENCY: complete

targets:
  VoiceCoach:
    type: application
    platform: iOS
    supportedDestinations:
      - iOS
      - macCatalyst
    sources:
      - VoiceCoach
    settings:
      base:
        GENERATE_INFOPLIST_FILE: YES
        MARKETING_VERSION: "1.0"
        CURRENT_PROJECT_VERSION: "1"
        INFOPLIST_KEY_NSCameraUsageDescription: "VoiceCoach records video of your practice sessions."
        INFOPLIST_KEY_NSMicrophoneUsageDescription: "VoiceCoach records audio during your practice sessions."
        INFOPLIST_KEY_NSPhotoLibraryUsageDescription: "VoiceCoach can save and import practice videos from your photo library."
        INFOPLIST_KEY_UILaunchScreen_Generation: true
        INFOPLIST_KEY_UISupportedInterfaceOrientations_iPhone: "UIInterfaceOrientationPortrait UIInterfaceOrientationLandscapeLeft UIInterfaceOrientationLandscapeRight"
        INFOPLIST_KEY_UISupportedInterfaceOrientations_iPad: "UIInterfaceOrientationPortrait UIInterfaceOrientationPortraitUpsideDown UIInterfaceOrientationLandscapeLeft UIInterfaceOrientationLandscapeRight"
        PRODUCT_BUNDLE_IDENTIFIER: com.voicecoach.app
        TARGETED_DEVICE_FAMILY: "1,2"

  VoiceCoachTests:
    type: bundle.unit-test
    platform: iOS
    supportedDestinations:
      - iOS
      - macCatalyst
    sources:
      - VoiceCoachTests
    dependencies:
      - target: VoiceCoach
    settings:
      base:
        GENERATE_INFOPLIST_FILE: YES
        PRODUCT_BUNDLE_IDENTIFIER: com.voicecoach.tests
